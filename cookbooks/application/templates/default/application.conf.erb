server {
  listen   80;
  server_name  <%= node[:hostname] %>;

  access_log  <%= node[:nginx][:log_dir] %>/application.log;

  location / {
    root   <%= node[:application][:dir] %>;
    index  index.html index.htm;
  }
}
